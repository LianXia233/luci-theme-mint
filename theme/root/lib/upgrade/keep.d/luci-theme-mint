# Written at RUNTIME, never by the package: these are the images the admin
# uploaded on the Appearance page. sysupgrade only preserves conffiles,
# /etc/sysupgrade.conf and the paths listed in a package's
# /lib/upgrade/keep.d/*, so without this file every firmware upgrade
# silently discards them.
#
# The wallpaper-*.img cache files are deliberately NOT listed - they are
# regenerated by the mz-wallpaper-fetch cron within one period.
/www/luci-static/mint/custom-pc.jpg
/www/luci-static/mint/custom-mobile.jpg
